<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.utng.database2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.FormBebidasActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"></action>
                <category android:name="android.intent.category.LAUNCHER"></category>
            </intent-filter>
        </activity>
        <activity android:name=".controller.ListaBebidasActivity"></activity>
        <activity android:name=".controller.FormClientesActivity"></activity>
        <activity android:name=".controller.ListaClientesActivity"></activity>
    </application>

</manifest>
